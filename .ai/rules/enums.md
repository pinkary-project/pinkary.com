---
paths:
  - 'app/Enums/**'
---

# Enums

## Backed enums with toArray() helper
Back enums by `string`, name cases in TitleCase, and add a `public static function toArray(): array` mapping value=>label for form/select consumption.
